// src/config.ts
// const BASE_URL = import.meta.env.VITE_API_URL || 'http://127.0.0.1:8000'; // Cambia la URL por defecto si no has desplegado
const BASE_URL = 'http://127.0.0.1:8000';

export const API_CONFIG = {
  baseURL: BASE_URL,
};
